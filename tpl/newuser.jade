extends layout

block content
    h1= title
    form#formAddUser(name="adduser",method="post",action="/adduser")
        input#inputUserName(placeholder="username", name="username")
        input#inputPassword(placeholder="password", name="password", type="password")
        button#btnSubmit(type="submit") submit
